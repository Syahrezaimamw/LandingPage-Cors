import React from 'react'

const ButtonTemp = ({children}) => {
  return (
    <button className='py-3 font-semibold text-white bg-pink-600 rounded-md px-14'>{children}</button>

  )
}

export default ButtonTemp
